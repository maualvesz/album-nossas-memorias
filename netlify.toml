[build]
  command = "npm install"
  functions = "netlify/functions"
  publish = "public"

[dev]
  command = "npm install && echo 'Dev server ready'"
  port = 8888

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
